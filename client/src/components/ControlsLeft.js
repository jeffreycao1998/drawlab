import React, { useState } from 'react';
import styled from 'styled-components';
import socket from '../socket';

const StyledControlsLeft = styled.div`
  grid-area: controlsLeft;
  height: 100%;
  width: 100%;
`;

const ControlsLeft = () => {


  return(
    <StyledControlsLeft>

    </StyledControlsLeft>
  )
};

export default ControlsLeft;