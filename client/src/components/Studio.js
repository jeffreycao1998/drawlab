import React from 'react';
import styled from 'styled-components';

const StudioContainer = styled.div`
  width: 100vw;
  height: 100vh;
  overflow: hidden;
  position: relative;
`;

const Studio = () => {
  return(
    <StudioContainer>

    </StudioContainer>
  );
};